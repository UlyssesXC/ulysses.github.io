# 🚀 快速入门指南

欢迎使用个人网站模板！按照以下步骤快速创建你的个人网站。

## 第一步：准备头像图片 📸

1. 准备两张头像照片（建议正方形，至少160x160像素）
   - 一张作为默认头像
   - 一张作为鼠标悬停时显示的头像（可以是另一个表情或角度）

2. 重命名图片：
   - `avatar.jpg` - 默认头像
   - `avatar-hover.jpg` - 悬停头像

3. 将这两张图片放入 `images/` 文件夹

## 第二步：修改个人信息 ✏️

编辑 `config.js` 文件，更新你的信息：

```javascript
const config = {
    avatar: {
        normal: 'images/avatar.jpg',
        hover: 'images/avatar-hover.jpg'
    },
    social: {
        github: 'https://github.com/你的用户名',
        scholar: 'https://scholar.google.com/citations?user=你的ID',
        email: '你的邮箱@example.com',
        linkedin: 'https://www.linkedin.com/in/你的用户名'
    }
};
```

## 第三步：编写内容 📝

编辑 `content/` 文件夹中的Markdown文件：

### 1. `about.md` - 关于我
写下你的简介、研究方向、兴趣爱好等。

### 2. `publications.md` - 我的文章
列出你的论文发表，包括标题、作者、发表会议/期刊等。

### 3. `projects.md` - 我的项目
介绍你的项目，可以包括开源项目、个人作品等。

### 4. `education.md` - 我的教育
填写你的教育背景，包括学位、学校、时间等。

### 📌 内容编写提示

- 使用 `###` 创建小标题
- 使用 `**文字**` 加粗
- 使用 `[链接文字](网址)` 添加链接
- 使用 `---` 创建分隔线

## 第四步：添加图片和GIF 🖼️

### 添加图片文件
将你的图片和GIF动画放到 `images/` 文件夹中。

### 在Markdown中引用

**基本用法：**
```markdown
![图片描述](images/你的图片.jpg)
```

**控制大小：**
```markdown
![小图[small]](images/图片.jpg)      # 300px宽
![中图[medium]](images/图片.jpg)    # 500px宽
![大图[large]](images/图片.jpg)     # 800px宽
```

**控制位置：**
```markdown
![左对齐[left]](images/图片.jpg)
![右对齐[right]](images/图片.jpg)
![居中[center]](images/图片.jpg)
```

**组合使用：**
```markdown
![演示动画[medium][center]](images/demo.gif)
```

## 第五步：运行网站 🌐

### 方法一：使用启动脚本（推荐）

```bash
./start.sh
```

### 方法二：手动启动

```bash
# 使用Python
python3 -m http.server 8000

# 或使用Node.js
npx http-server
```

### 方法三：使用VS Code扩展

安装 "Live Server" 扩展，然后右键点击 `index.html`，选择 "Open with Live Server"

### 打开网站

在浏览器中访问：`http://localhost:8000`

## 第六步：发布到网上 🌍

### 选项A：GitHub Pages（免费）

1. 创建GitHub仓库
2. 上传所有文件到仓库
3. 在仓库设置中启用 GitHub Pages
4. 你的网站将发布在 `https://你的用户名.github.io/仓库名/`

### 选项B：Netlify（免费）

1. 注册 Netlify 账号
2. 连接你的GitHub仓库
3. 点击部署
4. 获得一个 `https://你的网站.netlify.app` 域名

### 选项C：Vercel（免费）

1. 注册 Vercel 账号
2. 导入你的GitHub仓库
3. 自动部署完成

## 常见问题 ❓

### Q1: 直接双击index.html打开可以吗？
**A:** 不推荐。浏览器安全限制会阻止加载Markdown文件。请使用本地服务器。

### Q2: 如何更改网站颜色？
**A:** 编辑 `style.css` 文件，搜索颜色代码（如 `#3498db`）并替换为你喜欢的颜色。

### Q3: 可以添加更多页面吗？
**A:** 可以！编辑 `index.html` 添加新的section，创建对应的 `.md` 文件，并在 `config.js` 中配置路径。

### Q4: 图片不显示怎么办？
**A:** 检查：
- 图片文件是否在 `images/` 文件夹中
- 文件名是否正确（区分大小写）
- 是否使用了本地服务器运行

### Q5: GIF动画太大加载慢怎么办？
**A:** 可以使用在线工具压缩GIF：
- [ezgif.com](https://ezgif.com/optimize)
- [gifcompressor.com](https://gifcompressor.com/)

## 进阶技巧 🎨

### 1. 修改侧边栏颜色
编辑 `style.css`，找到：
```css
.sidebar {
    background-color: #2c3e50;  /* 改成你喜欢的颜色 */
}
```

### 2. 修改主题色
在 `style.css` 中搜索 `#3498db`（蓝色），全部替换为你的主题色。

### 3. 添加自定义字体
在 `style.css` 的 `body` 部分修改 `font-family`。

### 4. 添加网站图标
在 `index.html` 的 `<head>` 部分添加：
```html
<link rel="icon" href="images/favicon.ico">
```

## 需要帮助？ 💬

- 查看 `README.md` - 完整文档
- 查看 `EXAMPLES.md` - Markdown使用示例
- 检查浏览器控制台 - 看是否有错误信息

---

## 检查清单 ✅

完成设置前，确保：

- [ ] 已添加 `avatar.jpg` 和 `avatar-hover.jpg`
- [ ] 已修改 `config.js` 中的个人信息
- [ ] 已编辑所有 content/*.md 文件
- [ ] 已测试本地运行网站
- [ ] 所有链接都正确
- [ ] 所有图片都能正常显示
- [ ] 在手机浏览器中测试（响应式）

---

祝你创建出漂亮的个人网站！🎉

